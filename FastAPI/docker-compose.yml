services:
  api:
    build:
      dockerfile: ./app/Dockerfile
      context: .
    ports:
      - "80:80"
    depends_on:
      - db
    environment:
      - DB_USERNAME=dbadmin
      - DB_PASSWORD=Wolfros!#$134
      # - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=widgets_db

  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: dbadmin
      POSTGRES_PASSWORD: Wolfros!#$134
      # POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: widgets_db
    ports:
      - 5434:5432
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: